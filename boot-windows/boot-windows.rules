/* Allow wheel members to reboot without an additional password prompt.
 * Selecting Windows for the next boot requires authentication via pkexec.
 */
polkit.addRule(function(action, subject) {
    if (action.id == "org.freedesktop.login1.reboot" &&
        subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});